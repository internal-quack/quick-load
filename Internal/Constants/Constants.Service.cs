namespace QuickLoad
{
    internal static partial class Constants
    {
        // Messages
        internal const string InitScenePathNotSetMessage = "Init scene path is not set, go to project settings and select initialize scene";
        internal const string NetworkLoaderIsNullMessage = "Network loader is null, remember to assign it in Quick Load section in projects settings";
        
        // Tags
        internal const string HostTag = "Host";
        internal const string ClientTag = "Client";
    }
}
